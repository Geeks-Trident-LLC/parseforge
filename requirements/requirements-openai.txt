# Mirrors pyproject.toml's [project.optional-dependencies] "openai" extra --
# use `pip install parseforge[openai]` instead if pip's extras syntax works
# for your setup. For running parseforge's test suite against this
# provider, use dev-openai.txt instead (adds pytest/pytest-cov).
#
# Usage (from the repo root): pip install -r requirements/requirements-openai.txt

# Upper-bounded on textfsm-ai: 0.7.0 removed cost/pricing estimation
# entirely (a deliberate, permanent upstream change), which
# parseforge's cost.py depends on -- see pyproject.toml for details.
-e .
openai>=1.0.0
textfsm-ai[openai]>=0.6.1,<0.7.0
