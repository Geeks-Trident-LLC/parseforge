# Everything needed to install parseforge and run its test suite against
# the gemini naming provider, in one `pip install -r ...` -- no separate
# `pip install -e .` or `pytest`/`pytest-cov` install needed first.
# Mirrors pyproject.toml's [project.optional-dependencies] "gemini" extra
# plus the "dev" extra's test-running essentials (not its lint/format/
# typecheck tools -- this file is for testing, not development tooling).
#
# Usage (from the repo root): pip install -r requirements/requirements-gemini.txt

-e .
pytest>=8.0.0
pytest-cov>=5.0.0
google-genai>=0.2.0
textfsm-ai[gemini]>=0.6.1
