# Everything needed to install parseforge and run its test suite against
# the cohere naming provider, in one `pip install -r ...` -- no separate
# `pip install -e .` or `pytest`/`pytest-cov` install needed first.
# Mirrors pyproject.toml's [project.optional-dependencies] "cohere" extra
# plus the "dev" extra's test-running essentials (not its lint/format/
# typecheck tools -- this file is for testing, not development tooling).
#
# Usage (from the repo root): pip install -r requirements/requirements-cohere.txt
#
# cohere pinned exactly: 5.21.1 is the last release supporting Python 3.9
# (6.x/7.x require Python >=3.10), matching textfsm-ai's own pin.

-e .
pytest>=8.0.0
pytest-cov>=5.0.0
cohere==5.21.1
textfsm-ai[cohere]>=0.6.1
