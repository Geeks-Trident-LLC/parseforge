# Development tools only (runtime dependencies live in pyproject.toml)
-r requirements.txt

# Build & publish
build
twine

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Linting & formatting
ruff>=0.4.0
black>=24.0.0

# Type checking
mypy>=1.10.0
